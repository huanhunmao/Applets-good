<view class="container" hidden="{{hidden}}">
    <image class="classic-img" src="{{img}}" />
    <image class="play-img" src="{{playSrc}}" />
    <image class="tag" src="images/music@tag.png" />
    <text class="content">{{content}}</text>
</view>